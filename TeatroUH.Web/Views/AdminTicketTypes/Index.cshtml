@model IEnumerable<TeatroUH.Domain.Entities.TicketType>
@{
    ViewData["Title"] = "Tipos de Ticket";
}

<div class="page-header">
    <div>
        <h2 class="page-title">Tipos de Ticket</h2>
        <p class="page-subtitle">General/VIP y factor de precio.</p>
    </div>

    <a asp-action="Create" class="btn btn-primary">+ Nuevo tipo</a>
</div>

<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-dark table-hover table-admin">
                <thead>
                    <tr>
                        <th>Nombre</th>
                        <th class="text-end">Factor</th>
                        <th>Estado</th>
                        <th class="text-end">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var t in Model)
                    {
                        <tr>
                            <td>@t.Name</td>
                            <td class="text-end">@t.PriceFactor</td>
                            <td>
                                @if (t.IsActive)
                                {
                                    <span class="badge bg-success">Activo</span>
                                }
                                else
                                {

                                    <span class="badge bg-secondary">Inactivo</span>
                                }
                            </td>
                            <td class="text-end">
                                <a asp-action="Edit" asp-route-id="@t.TicketTypeId" class="btn btn-sm btn-outline-light">
                                    Editar
                                </a>

                                <form asp-action="ToggleActive" asp-route-id="@t.TicketTypeId" method="post" class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm @(t.IsActive ? "btn-outline-warning" : "btn-outline-success")">
                                        @(t.IsActive ? "Desactivar" : "Activar")
                                    </button>
                                </form>

                                <form asp-action="Delete" asp-route-id="@t.TicketTypeId" method="post" class="d-inline"
                                      onsubmit="return confirm('¿Eliminar este tipo de ticket?');">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-outline-danger">
                                        Eliminar
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
